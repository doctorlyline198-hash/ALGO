{
  "name": "algo-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10"
  },
  "lint-staged": {
    "client/src/**/*.{js,jsx,ts,tsx,css}": "node scripts/run-client-tests.cjs",
    "server/src/**/*.js": "npm --prefix server run test"
  }
}
